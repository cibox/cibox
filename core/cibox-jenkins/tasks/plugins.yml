---

# Install Jenkins plugins
- name: Install/update plugins
  sudo: yes
  action: command java -Djenkins.install.runSetupWizard=false -jar {{ jenkins.cli_dest }} -s http://localhost:8080/jenkins install-plugin {{ item }}
  with_items: plugins
  notify:
    - 'Restart Jenkins'
    
